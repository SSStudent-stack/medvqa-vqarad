# Core utilities
tqdm>=4.66
Pillow>=10.0

# Baseline (PyTorch + torchvision)
torch>=2.1
torchvision>=0.16

# BLIP (Transformers)
transformers>=4.35

# Metrics (BLEU in metrics.py)
nltk>=3.8

# Optional: suppress LibreSSL warning on some macOS Python builds
urllib3<2
